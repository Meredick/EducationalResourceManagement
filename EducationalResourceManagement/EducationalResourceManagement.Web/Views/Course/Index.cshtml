<table class="course-list-table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Teacher</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>Math</td>
            <td>John Doe</td>
            <td class="actions">
                <a href="@Url.Action("Details", "Courses", new { id = 1 })">Details</a>
                <a href="@Url.Action("Edit", "Courses", new { id = 1 })">Edit</a>
                <form method="post" action="@Url.Action("Delete", "Courses", new { id = 1 })" style="display:inline;">
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </td>
        </tr>
        <tr>
            <td>Science</td>
            <td>Jane Smith</td>
            <td class="actions">
                <a href="@Url.Action("Details", "Courses", new { id = 2 })">Details</a>
                <a href="@Url.Action("Edit", "Courses", new { id = 2 })">Edit</a>
                <form method="post" action="@Url.Action("Delete", "Courses", new { id = 2 })" style="display:inline;">
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </td>
        </tr>
    </tbody>
</table>

<a href="@Url.Action("Create", "Courses")" class="create-course-btn">Create New Course</a>
